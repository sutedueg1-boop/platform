# HSE Platform - Elsewedy Polymers
## Comprehensive Technical Architecture & Project Documentation

### 1. نظرة عامة على المشروع (Project Overview)
- **الغرض الأساسي من المنصة:** نظام متكامل لإدارة السلامة والصحة المهنية (HSE)، تصاريح العمل (Work Permits)، التدريب والتوعية (Safety Training)، وبلاغات الخطورة (Hazard Management).
- **الجهة التابعة:** السويدي للبوليمرات (Elsewedy Polymers).
- **القيمة المضافة:** يهدف المشروع إلى توفير بيئة عمل آمنة من خلال أتمتة وتسريع إجراءات السلامة، وتقليل الحوادث عبر المتابعة الفورية للأعمال الحرجة والتعامل الاستباقي مع المخاطر، بالإضافة إلى تنظيم أرشيف رقمي مركزي لكل ما يخص التدريب وحوادث العمل.

---

### 2. البنية التقنية (Tech Stack & Architecture)
- **Backend:** مبني باستخدام Node.js مع إطار العمل Express.js لإنشاء واجهات برمجية (RESTful APIs) مرنة وسريعة. يعتمد على أنظمة الجلسات (Sessions) وتقنية JWT (JSON Web Tokens) للتوثيق وتأمين نقاط النهاية (Endpoints).
- **Frontend:** مطور باستخدام Vanilla JavaScript (ES6+)، معمارية HTML5، وتنسيقات Custom CSS3 بدون إطارات عمل ثقيلة لضمان السرعة. يدعم التطبيق تقنيات PWA (Progressive Web App) ليعمل كتطبيق هاتف.
- **Libraries & Tooling:** 
  - `xlsx` (SheetJS) للتعامل مع استيراد وتصدير بيانات الموظفين والتقارير.
  - تقنيات Web Push و Service Workers لدعم الإشعارات الفورية (Real-time Push Notifications).
- **Storage/Persistence:** 
  - حفظ البيانات يتم محلياً في ملفات JSON (مثل `employees.json`، `hazards.json`، `trainings.json`، الخ) تعمل كقاعدة بيانات مدمجة (Embedded DB).
  - يتم تخزين البيانات بشكل متزامن مع الذاكرة العشوائية (In-memory) أثناء التشغيل لضمان استجابة فائقة السرعة، مع آلية حفظ دورية (Write Queue) لحماية البيانات من التلف.

---

### 3. أدوار وصلاحيات المستخدمين (Role-Based Access Control - RBAC)

| الدور (Role) | الوصف والصلاحيات المتاحة |
| --- | --- |
| `worker` (العامل / الفني) | تسجيل الدخول بكود الموظف، إنشاء البلاغات، الاطلاع على سجل التدريبات الخاص به، وطلب تصاريح عمل مبدئية. |
| `super_admin` (مدير النظام الشامل) | تحكم كامل في كافة وحدات النظام، إدارة المستخدمين، النسخ الاحتياطي، حذف وتعديل البلاغات والتصاريح. |
| `hse_admin` (مسؤول ومشرف السلامة) | مراجعة واعتماد تصاريح العمل، توجيه بلاغات الخطورة للصيانة، إدارة المحاضرات التدريبية، وإصدار التقارير. |
| `dept_admin` (مشرف القسم) | الإشراف على تصاريح وبلاغات قسمه فقط، والتصديق على الإجراءات المتعلقة بمنطقته. |
| `maint_admin` (أقسام الصيانة) | رؤية البلاغات الموجهة لقسمه (ميكانيكية، كهربائية، وقائية)، بدء المعالجة الفنية، إغلاق البلاغات أو رفضها مع إبداء الأسباب. |

---

### 4. الوحدات الوظيفية ودورات العمل (Core Modules & Workflows)

#### أ. إدارة تصاريح العمل (Work Permits Module)
- **آلية إنشاء التصريح:** تعبئة نموذج يحتوي على الحقول الأساسية (المقاول، القسم، نوع العمل، المعدات المستخدمة، تقييم المخاطر).
- **دورة حياة الاعتماد:** يمر التصريح بعدة مراحل: (قيد الانتظار `Pending` -> اعتماد المشرف `Approved` -> سارٍ `Active` -> مغلق/ملغي `Closed/Canceled`).
- **تصدير وأرشفة:** إمكانية تصفية السجلات وتصديرها بصيغة Excel، مع حفظ أرشيف كامل يسهل الرجوع إليه.

#### ب. بلاغات الخطورة وتوجيه الصيانة (Hazard Management & Maintenance Dispatch)
- **تقديم البلاغ:** يتيح للعامل أو المشرف تقديم وصف وصورة للخطر المحتمل.
- **تقييم الخطورة:** يتم حساب مستوى الخطورة آلياً (Risk Matrix Calculator) بناءً على الاحتمالية والشدة (High, Medium, Low).
- **دورة عمل التوجيه للصيانة (Workflow Engine):**
  - **التوجيه:** يقوم مسؤول السلامة باختيار القسم الفرعي (Electrical, Mechanical, Preventive).
  - **تحديث الواجهة:** بمجرد التوجيه، يتم تجميد زر "توجيه للصيانة" وتظهر شارة `تم التوجيه لـ: [القسم]`.
  - **التتبع الزمني (Timestamps):** تسجيل فوري لكل خطوة: وقت الإرسال (`submittedAt`)، المشاهدة (`seenAt`)، وبدء المعالجة الفعلي (`treatmentStartedAt`).
  - **الإنهاء التلقائي (Auto-completion Rule):** إذا قامت الصيانة بإغلاق البلاغ مباشرة دون الضغط على "بدء"، يتم مساواة وقت البدء بوقت الإغلاق (`completedAt`) لضمان عدم وجود حقول فارغة.
  - **الخيارات:** يمكن لمدير الصيانة قبول البلاغ والبدء فيه، أو رفضه مع ذكر السبب ليعود تلقائياً لمسؤول السلامة.

#### ج. التدريب والتوعية (Safety Training & Lectures Module)
- **واجهة العامل:** عرض نسبة الحضور الإجمالية وسجل الدورات السابقة، مع إمكانية الانضمام للمحاضرات النشطة عبر إدخال كود الدورة (PIN).
- **واجهة الإدارة:** تتيح لمشرف السلامة إنشاء محاضرة جديدة، عرض قائمة الحضور في البث المباشر (Live Sessions)، وتصدير كشوف الحضور.
- **أتمتة بيانات المحاضر:** يقوم النظام آلياً باستخراج (السحب الآلي) لاسم وكود المحاضر من توكن الجلسة (`sessionStorage`) أو من قاعدة الموظفين وتأمينها كحقول للقراءة فقط (`readonly`) لمنع التلاعب.

#### د. دليل الموظفين وإدارة الحسابات (Employee Directory & Users)
- **البيانات:** إدارة شاملة للقوة العاملة، تدعم الاستيراد المجمع وتصدير البيانات عبر ملفات Excel.
- **الربط الذكي:** يربط النظام الأكواد الوظيفية بأسماء الموظفين والأقسام تلقائياً (Active Employee Resolver) لضمان الدقة في تعيين المدربين، المبلغين، والمشرفين.

---

### 5. دورة التوثيق وتأمين الجلسات (Authentication & Session Architecture)
- **نظام التوكن (JWT):** يتم إصدار رمز مميز (`wp_auth_token`) عند تسجيل الدخول، يُحفظ في `sessionStorage` لضمان انتهاء الجلسة عند إغلاق المتصفح.
- **فك التشفير الآمن:** يتم تحليل التوكن في الواجهة الأمامية عبر فك التشفير الآمن (Base64Url & UTF-8 bytes decoding) لاستخراج هوية وصلاحيات المستخدم الحالي.
- **تحديد هوية الموظف:** يعتمد النظام على خوارزمية ذكية (Active Employee Resolver) للبحث عن وتوثيق هوية الموظف باستخدام الكود الوظيفي (`empCode`) كمعرف رئيسي بدلاً من الاسم.

---

### 6. مسارات الـ API (API Endpoints Reference)

| المسار (Endpoint) | الطريقة (Method) | الغرض من المسار |
| --- | --- | --- |
| `/api/worker-login` | `POST` | تسجيل دخول العمال والفنيين باستخدام الكود الوظيفي. |
| `/api/login` | `POST` | مصادقة المشرفين والإدارة وإصدار الـ JWT. |
| `/api/permits` | `GET` / `POST` | جلب قائمة التصاريح أو إنشاء تصريح عمل جديد. |
| `/api/hazards` | `GET` / `POST` | جلب البلاغات وإنشاء بلاغات الخطورة. |
| `/api/hazards/:id` | `PATCH` | تحديث حالة البلاغ (توجيه للصيانة، بدء المعالجة، الإغلاق). |
| `/api/training` | `GET` / `POST` | إدارة المحاضرات التدريبية وإنشاء جلسات جديدة. |
| `/api/employees` | `GET` / `POST` | استرداد وإدارة دليل الموظفين. |
| `/api/users` | `GET` / `POST` | إدارة حسابات المشرفين وصلاحيات الـ RBAC. |

---

### 7. خطوات التشغيل والتثبيت (Deployment & Setup Guide)
- **المتطلبات المسبقة:** تأكد من تثبيت بيئة Node.js (يُفضل الإصدار 16 فما فوق).
- **خطوات التشغيل المحلي:**
  1. استنساخ المشروع (Clone Repository).
  2. تشغيل الأمر `npm install` لتحميل الحزم والاعتماديات.
  3. تشغيل الأمر `npm start` لبدء الخادم (Server).
- **استكشاف الأخطاء:** 
  - في حال واجهت خطأ `EADDRINUSE` (المنفذ 3000 قيد الاستخدام)، قم بإنهاء عملية Node القديمة عبر إدارة المهام أو باستخدام الأمر في PowerShell:
    `Get-Process -Name node -ErrorAction SilentlyContinue | Stop-Process -Force`، ثم أعد التشغيل.
- **إدارة الكاش (Cache-Busting):** 
  - يعتمد النظام على إضافة مُعامل نسخة للملفات الساكنة مثل `<script src="app.js?v=99.9.9" defer></script>` لضمان مسح النسخ القديمة من المتصفح (Browser Cache) عند رفع التحديثات الجديدة لبيئة الإنتاج.
